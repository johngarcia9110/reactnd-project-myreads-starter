import React, { Component } from 'react';
import Bookshelf from './Bookshelf';

class ListBooks extends Component {
    render(){
        const {bookList, shelves} = this.props;
        console.log(
            shelves
        )
        return(

        )
    }
}

export default ListBooks